#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
<<<<<<< HEAD
  exec "$basedir/node"  "$basedir/../eslint/bin/eslint.js" "$@"
else 
  exec node  "$basedir/../eslint/bin/eslint.js" "$@"
=======
  exec "$basedir/node"  "$basedir/../.pnpm/eslint@9.31.0/node_modules/eslint/bin/eslint.js" "$@"
else
  exec node  "$basedir/../.pnpm/eslint@9.31.0/node_modules/eslint/bin/eslint.js" "$@"
>>>>>>> auth/login
fi
